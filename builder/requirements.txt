# Required Python packages get listed here, one per line.
# Recommended to lock the version number to avoid unexpected changes.
#
# torch + torchvision: installed in Dockerfile from PyTorch CUDA wheels (not PyPI),
# so pip does not install the CPU-only torch build.
#
# Aligned with https://huggingface.co/spaces/not-lain/background-removal (minus Gradio/Spaces UI).

runpod==1.2.0
fastapi
pydantic
requests
uvicorn[standard]

accelerate
opencv-python
pillow
numpy
timm
kornia
scikit-image
huggingface_hub
transformers>=4.39.1
einops
